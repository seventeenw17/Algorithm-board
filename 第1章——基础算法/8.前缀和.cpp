// 一维
// a[]为原数组，s[]为前缀和数组
for (int i = 1; i <= n; i++)
    s[i] = s[i - 1] + a[i];
// 求l到r间的数字和
s[r] - s[l - 1]

// 二维
// a[][]为原数组，s[][]为前缀和数组
for (int i = 1; i <= n; i++)
    for (int j = 1; j <= m; j++)
        s[i][j] = s[i - 1][j] + s[i][j - 1] - s[i - 1][j - 1] + a[i][j];
// 求(x1, y1)到(x2, y2)的区域和
s[x2][y2] - s[x2][y1 - 1] - s[x1 - 1][y2] + s[x1 - 1][y1 - 1]
